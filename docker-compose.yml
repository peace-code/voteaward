app:
  build: .
  command: rails server -p 3000 -b '0.0.0.0'
  volumes:
    - .:/app
  ports:
    - "3000:3000"
  external_links:
    - my-local-mongo
  # links:
  #  - mongo
# mongo:
  # image: mongo:3.0
  # ports:
  #   - "27017"
